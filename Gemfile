source 'https://rubygems.org'

# Needed because of private dependency: ws-gem_publisher.
source 'https://nexus.iad.w10external.com/repository/gems'

group :development, :test do
  gem 'bundler-audit', require: false
end

gemspec
